# Session Log — 2026-06-20 16:10

## Goal
Make the project **100% Pythonic**: translate the two non-Python computational notebooks
(c02 in R, c04 in Stata) to Python. The Google Earth Engine app stays JavaScript (out of scope).
c01, c03, c05, c06 were already Python.

## Current state
All computational notebooks are now Python. Full clean render succeeded (exit 0); all four
manuscript formats and all five notebook previews regenerated. Both new notebooks execute on the
`project2025s` kernel and embed correctly into `index.qmd`.

## Work done

### Pre-flight
- **Backup** of the R/Stata originals (copy, not move) → `archive/notebooks-pre-python-20260620/`
  (4 files: c02 + c04 `.ipynb` and `.md`). Placed *outside* `legacy/` to honor the sacred-legacy rule.
  Note: originals also live inside `legacy/project2025s-v*.zip`.
- **`uv add spreg`** → `spreg>=1.8.5` now declared in `pyproject.toml` (was only transitively installed
  via mgwr). Also added `spreg==1.8.5` to `requirements.txt` (Colab parity).
- **Registered the `project2025s` Jupyter kernel** (`ipykernel install --user`), which was missing in
  this environment. It points to `.venv/bin/python3`; c01/c06 already referenced it.

### c02: R → Python (regional convergence)
- Rewrote `notebooks/c02_regional_convergence_sc.md`: `pandas.read_stata` + `statsmodels` OLS
  (`light_growth96_10rcr_cap ~ log_light96_rcr_cap`) + `seaborn.regplot` (line + 95% CI band),
  outlier labels via `ax.annotate`, slope/R² annotation box (built with `.format()` to dodge the
  Python 3.10 f-string limitation). Prose preserved verbatim; caption preserved verbatim.
- **Verified vs published:** N=520, R²=**0.255**, slope=**-0.0199** (→ -0.02), figure renders with
  3 outlier labels (Bharuch, Chamoli, Rudraprayag). Matches the original R result.

### c04: Stata → Python (spillover modeling, Table 1)
- Rewrote `notebooks/c04_spillover_modeling_6nn.md` by porting the working
  `replicate_india_sdm_convergence.py`: 6NN row-standardized weights via `libpysal.KNN`,
  SDM via `spreg.ML_Lag(slx_lags=1, spat_impacts="simple")`, OLS via statsmodels (HC1),
  Direct/Indirect/Total impacts with Monte-Carlo SEs (seed 20250620). Stata-kernel tutorial prose
  dropped; econometric narrative preserved.
- **Table 1 mechanism switched from `{{< include >}}` to `{{< embed #tbl-models >}}`**: the notebook
  emits a `#| label: tbl-models` cell that builds a Markdown pipe table via `IPython.display.Markdown`.
  `index.qmd:387` now embeds it. Renders as a proper HTML `<table>` with caption + working
  `Table 1` cross-reference; also present in PDF.
- The orphaned `tables/c04_spillover_modeling_table.md` is **kept** (no-delete rule), now unused.

### IMPORTANT — numbers shifted slightly (≤0.001) vs the old Stata table
The Python pipeline reproduces the standalone script **exactly**, but differs from the published
Stata column by ≤0.001 in a few SDM cells and by 2–4 in some AIC values. **This gap is the
spreg-vs-Stata estimator, NOT the weights** — verified by re-running the SDM with the *precomputed*
`data/W_matrix.csv`, which gives results identical to the libpysal recompute (M3 Direct -0.0250,
M4 -0.0244). There is no Python configuration that yields the Stata -0.026. Signs, significance
stars, percentages (48%/64%), and all conclusions are unchanged.

To keep the manuscript internally consistent with its now-Python table, I synced these hardcoded
numbers in `index.qmd`:
- L384: direct range "-0.020 to -0.026" → "-0.020 to **-0.025**"
- L391: conditional directs "-0.025 and -0.026" → "**-0.024 and -0.025**"
- L399: indirect "-0.015 (M3) / -0.012 (M4)" → "**-0.016 / -0.013**"
- L409: AIC "-2499 / -2469" → "**-2501 / -2465**"
- L411: AIC drop "345" → "**347**"
- L403/L404 (totals -0.037, -0.041; OLS -0.025; 48%/64%) unchanged — still exact.

### Housekeeping
- `CLAUDE.md`: language table c02 R→Python, c04 Stata→Python; `**Tools:**` line dropped R/Stata;
  added the c04 table to the "Current embeds" list.
- No changes needed to `_quarto.yml`, `jupytext.toml`, or `scripts/clean-render.sh`.

## Key results (Table 1, Python-computed)
| | M1 OLS/SDM | M2 OLS/SDM | M3 OLS/SDM | M4 OLS/SDM |
|--|--|--|--|--|
| Direct | -0.020/-0.021 | -0.022/-0.021 | -0.025/-0.025 | -0.025/-0.024 |
| Indirect | –/-0.001 | –/-0.001 | –/-0.016* | –/-0.013* |
| Total | -0.020/-0.022 | -0.022/-0.022 | -0.025/-0.041 | -0.025/-0.037 |
| AIC | -1945/-2292 | -2409/-2468 | -2211/-2358 | -2465/-2501 |

## Verification
- Both notebooks execute cleanly on `project2025s`; `bash scripts/clean-render.sh` → exit 0.
- HTML: c02 figure embedded; c04 renders as a real HTML table with caption + `#tbl-models` xref.
- PDF/REGION-PDF/DOCX all regenerated; PDF text contains the table.
- No R/Stata kernels or `language_info` remain in any notebook; no polluted cell metadata.

## Vestigial (retained per no-delete rule)
`project2025s.Rproj`, `data/Cultural_Data_India/CulturalData.do`, the `ir`/`stata` Jupyter kernels,
and `replicate_india_sdm_convergence.py` (the source for the c04 port). The `ir`/`stata` kernels are
no longer needed for rendering.

## Next steps / open items
- Author review of the 6 prose number edits in `index.qmd` (scientific text in their manuscript).
- Optional: reconcile the drifted `requirements.txt` more fully against `pyproject.toml`
  (it still lacks splot, tabulate, stargazer, jupytext) — out of scope this session.
