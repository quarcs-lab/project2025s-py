# Session Log — 2026-06-20 16:48

## Goal
Follow-up to the Python translation (see `20260620_1610.md`): (1) make the **main** Table 1 spatial
impacts use the **`full`** (LeSage–Pace) method, and (2) add a **notebook-only robustness table**
comparing `full`/`simple`/`power` for the preferred Model 4.

## Work done (c04 only)

### Main table → full method
- `notebooks/c04_spillover_modeling_6nn.md`:
  - Setup cell: precompute `EIGS_W = eigvals(Wd)`, `N_W`, `TR_WP` (tr(W^p), p=1..30).
  - New helper `_adi(method, rho)` → average direct-impact multiplier: simple=1; full=`mean_i 1/(1−ρλ_i)`;
    power=`1 + Σ_p ρ^p·tr(W^p)/n`. **Verified `_adi('full',ρ)` == spreg `sp_multipliers['full']` to ~1e-16.**
  - `run_sdm` switched from `simple` to `full`: `Direct = adi·β_own` (point via `_adi`, MC SE recomputes
    `adi` per ρ draw). Total & AIC unchanged (Total is method-invariant under row-standardized W).
  - Fixed the inaccurate docstring/prose claim that `simple` reproduces Stata — it's `full` that does.

### Robustness section (notebook only, Model 4)
- New `## 5` section + `tbl-impacts-robustness` cell: re-fits Model 4 with
  `spat_impacts=['simple','full','power']`, reports key-regressor Direct/Indirect/Total per method with
  Monte-Carlo SEs + stars. **Not embedded in index.qmd** (appears only on the c04 preview page).

## Key result — full method moves Python BACK toward the original Stata values
The `full` method **reproduces the original Stata table for Models 2, 3, 4** (e.g. M3 SDM Direct −0.026,
Indirect −0.015; M4 −0.025 / −0.013), confirming Stata's `estat impact` uses the full method.

### ⚠️ Model 1 changed substantively (needs author review)
Model 1 (unconditional, no FE) has a **high ρ ≈ 0.80** → `adi_full` ≈ 1.21, so the full method gives:
- M1 SDM **Direct −0.026** (was −0.021 under simple)
- M1 SDM **Indirect +0.004** — **sign flips to positive** (insignificant; was −0.001)
- (M1 Total −0.022 and AIC unchanged)

This is the genuine, verified full-method output (the small SLX coefficient is +0.017 and the large
multiplier inflates the direct effect). It complicates the manuscript's "unconditional smaller →
conditional larger" narrative because the M1 SDM direct now equals the conditional magnitudes.

### Table 1 (full method, as rendered)
| | M1 OLS/SDM | M2 OLS/SDM | M3 OLS/SDM | M4 OLS/SDM |
|--|--|--|--|--|
| Direct | -0.020/-0.026 | -0.022/-0.021 | -0.025/-0.026 | -0.025/-0.025 |
| Indirect | –/+0.004 | –/-0.001 | –/-0.015* | –/-0.013* |
| Total | -0.020/-0.022 | -0.022/-0.022 | -0.025/-0.041 | -0.025/-0.037 |
| AIC | -1945/-2292 | -2409/-2468 | -2211/-2358 | -2465/-2501 |

### Robustness (Model 4 key regressor)
| Method | Direct | Indirect | Total |
|--|--|--|--|
| full (main) | -0.025*** | -0.013* | -0.037*** |
| simple | -0.024*** | -0.013* | -0.037*** |
| power | -0.025*** | -0.013* | -0.037*** |

## Manuscript prose re-synced (`index.qmd`)
- L384: direct range "-0.020 to -0.025" → "**-0.020 to -0.026**".
- L390: added a clause noting the M1 SDM direct (−0.026) is amplified by strong residual spatial dependence.
- L391: conditional directs "-0.024 and -0.025" → "**-0.025 and -0.026**".
- L399: indirect "-0.016 in Model 3" → "**-0.015**" (M4 −0.013 unchanged).
- L403/404 (totals −0.037/−0.041, 48%/64%) and L409/411 (AIC) are method-invariant — unchanged.

## Verification
- `_adi('full')` matches spreg exactly; full reproduces Stata for M2–M4.
- `bash scripts/clean-render.sh` → exit 0; Table 1 in index.html/index.pdf shows full values
  (M1 SDM −0.026 / +0.004 confirmed in both); robustness table renders on the c04 preview page;
  no embed errors.

## Model 1 narrative — REWORDED (done)
Replaced the earlier parenthetical with a transparent reframing (user-approved) in `index.qmd`:
- Direct-effects paragraph now anchors on the OLS attenuation story (-0.020/-0.022 → -0.025), notes the
  SDM matches in the conditional models (-0.026 / -0.025), and states the unconditional SDM decomposition
  is unreliable (ρ≈0.8 inflates Model 1's direct to -0.026).
- Indirect-effects paragraph now says the unconditional spillovers are "insignificant and erratic —
  negative in Model 2 but positive in Model 1 —" due to omitted-variable confounding; significant
  negative spillovers (-0.015, -0.013) emerge only with controls.
Re-rendered (clean-render, exit 0); new prose confirmed in index.html and index.pdf; Table 1 unchanged.

## Version control
Initialized git (`main`) and pushed the full project to a new **private** repo
**`quarcs-lab/project2025s-py`** (the canonical public `quarcs-lab/project2025s` is left untouched).
README updated with a "Project status" section and the new repository link. `.venv/` excluded via
`.gitignore`; `legacy/` and `data/` committed as-is per request (largest file 92 MB, under GitHub's limit).
