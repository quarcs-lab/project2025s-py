# Colab / pip fallback. The authoritative environment is pyproject.toml + uv.lock;
# these bounds are kept compatible with it (NumPy 2.x) rather than pinned to the
# retired NumPy 1.x stack. Google Colab already ships most of the geospatial
# packages below, so on Colab only `contextily` is normally missing.
numpy>=2.0
pandas>=2.2.0
matplotlib>=3.8.2
seaborn>=0.13.2
scikit-learn>=1.4.0
scipy>=1.11
jupyter>=1.0.0
jupytext>=1.19.1
statsmodels>=0.14.6
tabulate>=0.9.0
stargazer>=0.0.7

# Geospatial packages
geopandas>=1.1.2
libpysal>=4.13.0
esda>=2.7.0
spreg>=1.8.5
splot>=1.1.5
mgwr>=2.2.1
rasterio>=1.3.9
contextily>=1.5.0
mapclassify>=2.6.1
pyogrio>=0.12.1
folium>=0.20.0
pyarrow>=22.0.0
plotly>=6.5.2
